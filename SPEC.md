# Farm Defense - Expanded Edition 仕様書

この文書は、`index.html`、`script.js`、`trap.js` の現在の実装を基準にした内部仕様である。仕様変更時はコードと本書を同時に更新する。

テスト仕様、テストケース、テストの実行方法、実行ログとの照合基準については [TEST.md](TEST.md) を参照する。

本書（`SPEC.md`）を変更した場合は、変更内容を必ず [TEST.md](TEST.md) に反映すること。

## 1. 概要

- HTML5 Canvas 2D と Vanilla JavaScript で動作する農園防衛ゲーム。
- 資金で畑・研究所・タレット・罠を設置し、ドローンを購入する。壁と旧来のスロータイルはプレイヤー向け建築一覧には表示しない。
- Wave 中に敵から畑を守り、`CONFIG.maxWave`（初期値10）を全て終えると勝利。
- 全ての畑が破壊されるとゲームオーバー。
- ゲーム状態はページ内メモリで管理する。設定編集内容はメモリ上で保持し、`exportConfig()`は `param.js` へ貼り戻せる `savedConfig` 宣言をクリップボードへコピーする。`param.js` の更新と再読み込みは手動で行う。

## 2. 画面と操作

- Canvas は `#gameCanvas`、論理サイズ1440×720、30列×15行、1タイル48px。マップは従来の横向き表示から見た目上2倍に拡大する。表示幅は左側の余白を使って可変とし、右側の操作パネルは360pxを確保するため、一般的なデスクトップ幅ではページスクロールなしで表示する。
- 敵はマップ左端（`x=0`）から出現し、右方向へ進行して右端側の基地（通常は`x=29`）へ向かう。マップ定義の旧来の縦向き座標はランタイム初期化時に90度回転して使用する。
- ゲーム画面には Wave開始、建築、ドローン購入、取得済みバフ、選択オブジェクト情報を表示する。
- ヘッダーにはゲームレベル、マップ、進行速度の操作UIを並べて表示する。マップを変更すると確認なしでゲームをリセットする。
- テックツリーのタブ名と画面見出しは「テックツリー」とする。
- テックツリー画面には選択中ツリーの研究をTier別に表示する。
- パラメータ設定画面では共通設定、レギュレーション別の初期資金・Wave敵表・テックツリー、罠、発掘ドローン、バフ、Waveバフ、敵、敵能力、共通スプライトを編集できる。
- オーバーレイは、ゲーム開始時のテックツリー選択、Waveクリア時のバフ選択、勝敗表示に使用する。
- 建築モードではクリックまたはポインタードラッグで連続設置する。設置成功後も同じモードを維持する。
- 選択モードはWave開始ボタンの隣の専用ボタンで切り替える。選択モードでは畑または通常建物を選択し、アップグレードできる。建築一覧には撤去モードを表示し、通常建物をクリックすると撤去できる。通常建物を選択した場合は選択オブジェクト欄にも撤去ボタンを表示し、撤去時は建築コストを返還せず対象建物だけを削除する。畑は撤去機能の対象外とする。
- 建築可能範囲は、形状内かつ `x=1..gridCols-2`、`y=0..gridRows-1`。占有マス、経路を遮断するマスには設置できない。形状外には設置できない。

## 3. ゲーム状態と初期化

### 3.1 基本パラメータ

| キー | 初期値 |
|---|---:|
| `gridCols` / `gridRows` | 30 / 15 |
| `maxWave` | 10 |
| `initialMoney` | 501 |
| `blockedTileSpawnRate` | 0.15（マップの有効セルに対する発掘可能な移動不可マスの出現率。0〜1） |
| `waveSpawnInterval` / `waveDensityMultiplier` | 180ms / 1.25 |
| `slowTileEffect` | 0.5 |
| `costFarm` / `costWall` / `costLab` | 150 / 10 / 250 |
| `costSlow` / `costMG` / `costMissile` | 50 / 100 / 200 |
| `costDrone` | 150 |
| `cropSellValue` / `cropGrowSpeed` | 2 / 0.3 |
| `droneBaseSpeed` | 0.4 |
| `mgDamage` / `mgRange` / `mgCooldown` | 12 / 3.5 / 17 |
| `missileDamage` / `missileRange` / `missileCooldown` | 19 / 8 / 33 |
| `missileSplashRadius` | 3.2 |

敵の旧来パラメータ（`enemyBase*`、`runnerEnemy*`、`tankEnemy*`）は `CONFIG` に残っているが、最終的な敵生成では使用しない。敵は `ENEMY_DEFINITIONS` を使用する。

### 3.2 リセット時

1. 現在の難易度を `activeDifficultyLevel` に反映し、選択中レギュレーションの初期資金をそのまま資金にする。
2. Wave、敵、弾、エフェクト、ドローン、建物などのゲーム状態を初期化する。
3. 選択中マップを `configureActiveMap()` で再構成する。固定障害物に加え、`CONFIG.blockedTileSpawnRate` に応じた発掘可能な移動不可マスをランダム配置する。配置候補は全スポーン地点から基地への経路を保てる場合だけ採用し、敵が移動不能にならないようにする。
4. 選択中マップの基地に畑を1つ設置する。
5. `sow`、`water`、`harvest`、`excavation` のドローンを各1台生成する。
6. テックツリー選択オーバーレイを表示する。

初期マップは `twin_s` で、固定障害物はない。マップ初期化時にランダムな `blocked` マスが生成され、発掘ドローンはそれらを対象にする。出現率を0にするとランダムマスは生成されず、発掘ドローンはその場で待機する。固定障害物およびランダムな `blocked` マスは、描画上も移動上も障害物として扱われる。

## 4. マップと経路

### 4.1 マップ定義

`MAP_DEFINITIONS` に通常9マップと、表示確認専用の `display_test` を持つ。各マップは名称、1つ以上のスポーン地点、基地、形状生成用の軸方向中心線、任意の矩形障害物で構成される。中心線はマップの列・行の形状を生成するためだけに使用し、敵の固定移動経路ではない。`display_test` のみ、既存設定を変更せずに、全建物・全罠を各4個以上、各種ドローンを4機、高HP敵の大量Waveをマップ固有データとして初期配置する。さらに通路を横切る壁列を2か所に置き、各列に1マスの通過口を残して敵の迂回・再経路計算を確認できる。

デフォルトは `twin_s`。マップ選択変更時はゲームをリセットする。中心線を幅5セルに拡張した有効グリッドをマップ形状として描画し、形状外は移動・設置できない。マップ障害物が形状内にある場合は障害物として配置される。

次表は `MAP_DEFINITIONS` に記述する回転前の作者向け座標である。座標は `[列, 行]`、スポーンは行0、基地は通常行29にある。初期化時に `[行, 列]` へ90度回転するため、実行時にはスポーンが左端、基地が右端になる。

| ID | スポーン | 基地 | ルートの折れ点 |
|---|---|---|---|
| `straight` | main=[7,0] | [7,29] | [7,0]→[7,29] |
| `l_shape` | main=[2,0] | [12,29] | [2,0]→[2,18]→[12,18]→[12,29] |
| `basic_s` | main=[2,0] | [7,29] | [2,0]→[2,7]→[11,7]→[11,15]→[3,15]→[3,23]→[7,23]→[7,29] |
| `stairs` | main=[2,0] | [12,29] | [2,0]→[2,5]→[5,5]→[5,10]→[8,10]→[8,15]→[11,15]→[11,22]→[12,22]→[12,29] |
| `u_shape` | main=[3,0] | [7,29] | [3,0]→[3,20]→[11,20]→[11,8]→[7,8]→[7,29] |
| `parallel_double` | left=[3,0], right=[11,0] | [7,29] | 左右から[7,27]へ合流し[7,29]へ |
| `c_shape` | left=[2,0], right=[12,0] | [7,29] | 左右から[7,27]へ合流し[7,29]へ。障害物矩形[5,5]〜[9,23] |
| `split_area` | left=[3,0], right=[11,0] | [7,29] | 左右から[7,27]へ合流し[7,29]へ。障害物矩形[7,0]〜[7,27] |
| `twin_s` | left=[2,0], right=[12,0] | [7,29] | 左: [2,0]→[2,7]→[6,7]→[6,15]→[2,15]→[2,23]→[7,23]→[7,29] / 右: [12,0]→[12,7]→[8,7]→[8,15]→[12,15]→[12,23]→[7,23]→[7,29] |

ルートはデータ上の折れ点を `expandRoute()` で全セルへ展開する。斜めの折れ点は許可せず、定義に斜め区間があると例外になる。

### 4.2 経路探索

`findPath()` は A* ではなく、キューと `prev` Map を使う4方向 BFS である。移動コストとヒューリスティックによる優先度計算はない。範囲外、`grid` 上の値、ただし目的地自身を除く占有セルは通行不可。

敵の通常再経路計算は20フレームごと、経路なし、または30フレーム以上位置が変わらない場合に行う。敵は選択中マップの有効な列・行の範囲内で、スポーン地点から基地または畑まで4方向BFSで移動する。マップ定義の中心線を固定経路として優先しない。

建築時は、仮置きした壁が全スポーン地点から基地への経路を遮断しないことを確認する。既存の敵の現在位置からの経路は検証しない。

## 5. ゲームエンティティ

### 5.1 Farm

- `gx, gy, level, maxHp, hp, state, progress` を持つ。
- 初期HPは100。`farm_hp` バフ適用時は100×(1+バフ量)。
- `state`: 0=未播種、1=成長中、2=要水撒き、3=収穫可能。
- Wave中かつstate=1のとき、毎更新 `0.15 × cropGrowSpeed × (1+0.25×(level-1)) × (1+crop_speed)` を進め、100でstate=2にする。
- 畑アップグレード費は `floor(costFarm × 1.5 × level)`。レベル上昇後、HP上限を再計算して全回復する。

### 5.2 Building

- `wall` はHP150、その他の建物はHP100で開始する。
- `lab`: Wave中120フレームごとに `max(1, round(1 × (1+science_gen)))` 科学を生成する。
- `mg`: 射程3.5タイル、攻撃力12、発射間隔17フレーム。
- `missile`: 射程8タイル、攻撃力19、発射間隔33フレーム、半径3.2タイルの範囲攻撃。
- タレットは射程内の最寄りの敵へ弾を発射する。
- 建物アップグレード費は `floor(基礎建築費 × 0.8 × level)`。アップグレードでレベルを1増やし、HP上限・現在HPを1.35倍にする。

### 5.3 Drone

- `sow`: state=0の畑を探して播種。
- `water`: state=2の畑を探して水撒き。
- `harvest`: state=3の畑をstate=0へ戻し、`floor(cropSellValue × (1+crop_sell))` を獲得。
- `excavation`: `blocked` マスへ移動し、設定間隔ごとに発掘を試行する。
- 通常ドローンは目的地から4px以下で18フレーム作業する。
- Wave外は全ドローンが移動・作業を停止し、割当も解除する。
- ドローン購入費は、同タイプの所有数による1.5倍だけを適用する。

発掘ドローンの初期設定は、速度0.25、間隔180〜420フレーム、成功率0.65、資金20〜100、科学2〜12、バフ発見率0.25、発見個数1。成功時は資金と科学を獲得し、追加判定に成功すると未取得バフを獲得する。

### 5.4 Enemy

敵は `ENEMY_DEFINITIONS` の通常敵13種に加え、表示確認マップ専用の `showcase_titan` を持つ。`showcase_titan` は通常のレギュレーションWaveには登録せず、`display_test` マップ固有Waveでのみ使用する。

各定義は英語名、日本語名、分類、HP、速度、攻撃力、装甲率、罠耐性、能力IDを持つ。敵生成時は定義値をそのまま使用し、難易度・Wave倍率は適用しない。

初期値は次のとおり。

| ID | HP | 速度 | 攻撃力 | 装甲 | 罠耐性 | 能力 |
|---|---:|---:|---:|---:|---:|---|
| `peasant` | 20 | 1.2 | 4 | 0 | 0 | なし |
| `adventurer` | 35 | 1.2 | 6 | 0 | 0 | なし |
| `warrior` | 60 | 1.2 | 9 | 0.10 | 0.10 | なし |
| `thief` | 30 | 2.7 | 5 | 0 | 0 | なし |
| `horseman` | 70 | 3.0 | 11 | 0.15 | 0.25 | なし |
| `knight` | 120 | 0.75 | 15 | 0.30 | 0.35 | なし |
| `brute` | 240 | 0.55 | 24 | 0.35 | 0.50 | なし |
| `priest` | 40 | 1.2 | 5 | 0 | 0 | `heal_aura` |
| `drummer` | 65 | 1.2 | 7 | 0 | 0 | `speed_aura`, `attack_aura` |
| `shieldmaster` | 130 | 0.75 | 14 | 0.20 | 0.35 | `front_shield` |
| `wizard_speed` | 40 | 1.2 | 4 | 0 | 0 | `wizard_speed` |
| `wizard_attack` | 40 | 1.2 | 4 | 0 | 0 | `wizard_attack` |
| `wizard_heal` | 40 | 1.2 | 4 | 0 | 0 | `wizard_heal` |

敵能力設定の初期値は、Priest回復量5・間隔120・射程3、Drummerオーラ射程4・速度/攻撃倍率+20%、Wizardオーラ射程4・速度/攻撃倍率+25%、Wizard回復量4・間隔90、Shieldmaster前方軽減80%・角度120度である。距離の射程はタイル単位、回復間隔は更新フレーム単位。

- 最寄りの畑を目標にする。
- 畑から24×0.8px以内では移動せず、毎フレーム `damage/60` を与える。
- `armor` は通常被ダメージを最大95%まで軽減。
- `trapResistance` は罠効果量と持続時間を割合軽減。
- Priest/Wizardは範囲内の味方を一定間隔で回復する。
- DrummerとWizardは範囲内の他の敵へ速度または攻撃力バフを付与し、同種効果は最大値を採用する。
- Shieldmasterは移動方向前方の角度120度以内からのダメージを最大80%軽減する。

### 5.5 弾とエフェクト

弾速は5px/更新。マシンガンは単体、ミサイルは着弾点周囲の全敵へダメージを与える。`TextEffect` は獲得表示、`Explosion` はミサイル爆発表示に使用する。

## 6. 罠システム

処理は `trap.js` の `TrapEngine` が担当する。コインプッシャー型レギュレーションの初期罠は、`tile` 4種（`slow`、`poison`、`burn`、`armor_down`）と `line` 2種（`pull`、`knockback`）の計6種。旧来の3種（ライン・床面・広域）は使用しない。

対応効果は `slow`、`poison`、`burn`、`armor_down`、`stun`、`knockback`、`pull`。罠定義にはID、名称、タイプ、コスト、クールダウン、タイプ別の射程・半径・持続時間、1つ以上の効果を設定する。

初期定義は次のとおり。

| ID | タイプ | コスト | CD | 追加パラメータ | 効果 |
|---|---|---:|---:|---|---|
| `trap_tile_slow` | tile | 60 | 0.75秒 | 持続2秒（※罠本体の寿命には未使用） | slow:量0.45、2秒 |
| `trap_tile_poison` | tile | 80 | 1秒 | 持続4秒（※罠本体の寿命には未使用） | poison:量2、間隔1秒、4秒 |
| `trap_tile_burn` | tile | 85 | 1.2秒 | 持続3秒（※罠本体の寿命には未使用） | burn:量3、間隔0.75秒、3秒 |
| `trap_tile_armor_down` | tile | 75 | 1.5秒 | 持続3秒（※罠本体の寿命には未使用） | armor_down:量0.25、3秒 |
| `trap_line_pull` | line | 90 | 2秒 | 射程7、幅1 | pull:量1.5、0.25秒 |
| `trap_line_knockback` | line | 95 | 2秒 | 射程7、幅1 | knockback:量1.5、0.25秒 |

罠の距離判定はピクセル換算する。`line` は設置時に選択した `up`、`down`、`left`、`right` の方向へ罠セル中心から伸びる線分だけを対象とする。方向未指定の既存定義は `down` として扱う。`slow` は効果量を速度低下率として使用し、実効速度倍率は `max(0.05, 1 - amount)`、値が大きいほど速度が低下する。`stun` は敵の通常更新を停止する。毒・火傷は指定間隔ごとにダメージを与える。ノックバック・引き寄せはライン罠では設定方向（引き寄せは逆方向）、その他の罠では罠中心を基準に即時移動させ、Canvas内にクランプする。

- `line`: 設置時に選択した方向へ伸びる直線上。
- `tile`: 設置マス上の敵。Wave中は毎更新適用。
- `area`: 半径内の全敵。
- クールダウン、効果時間、毒・火傷の発動間隔は設定画面では秒で指定する。ゲーム速度1xでは1秒を60更新として処理し、速度変更時はゲーム内時間の進行に合わせて更新回数だけが増減する。
- tile以外はクールダウンが0のときに適用し、適用後に定義のクールダウンへ戻す。
- 同じ効果が既にある場合は新しい効果で置換する。
- 設置時に定義をコピーするため、後から編集しても設置済み罠は変わらない。
- 同じマスへの罠の重複設置は不可。罠IDは画面上で変更できない。
- 罠本体は設置後に自動消滅しない。罠定義の `duration` は床罠の表示寿命には使われず、付与する効果の持続時間として各効果の `duration` が使われる。
- 罠の購入価格は罠定義の固定値を使用し、難易度倍率は適用しない。

## 7. Wave と報酬

Wave開始時にWave番号を増やし、選択中レギュレーションの現在難易度にある`waves[wave]`の敵表を使用する。各敵IDの実出現数は `floor(設定数 × CONFIG.waveDensityMultiplier)` であり、初期値は1.25倍である（最低数補正はない）。敵は `CONFIG.waveSpawnInterval`（初期値180ms）間隔で、マップのスポーン地点を順番に使用して生成する。`display_test` はマップ固有の敵表と12ms間隔でこれを上書きする。

全敵が消滅し、スポーンも終了するとWaveクリア。最終Waveなら勝利、そうでなければ `WAVE_BUFF_CONFIG` に従って未取得バフから候補を提示する。初期設定は出現3、レアリティ重みR1=2/R2=1、取得1。候補数・取得数・重みはWave単位で編集できる。

Wave表はレギュレーションごと・難易度ごとにWave 1〜`CONFIG.maxWave`を必須とする。未記載Wave、空オブジェクト、未知の敵ID、`maxWave`超過は起動エラーとする。各Waveは敵合計1体以上とし、個々の敵数は0以上の整数とする。

Waveクリア処理では、まずWave終了イベントを発火し、少数建築ボーナスなどによる追加取得数を一時的に加算する。その後、未取得バフから候補を作る。候補は同じバフを重複させず、候補数は未取得バフ数を上限とする。取得済みバフを選んだ場合は再取得せず、選択数だけを減らす。`instant_money` は選択確定時に額面を資金へ加算する。

## 8. テックツリーとバフ

ゲーム開始時に `TECH_TREES` から1ツリーを選ぶ。初期ツリーは `standard` と `expedition`。研究データは `[id, name, tier, scienceCost, prerequisites, buffIds]`。前提を全て解除し、科学が足りている場合に研究を解除し、カンマ区切りのバフIDを取得する。

基礎研究は次のとおり。

| ツリー | ID | Tier | 科学 | 前提 | バフ |
|---|---|---:|---:|---|---|
| standard | `tech_agriculture` 近代農業 | 1 | 50 | なし | `buff_crop_speed` |
| standard | `tech_defense_1` 簡易防衛線 | 1 | 50 | なし | `buff_mg_damage` |
| standard | `tech_irrigation` 市場流通改善 | 2 | 120 | agriculture | `buff_crop_sell` |
| standard | `tech_ballistics` 自動装填機構 | 2 | 150 | defense_1 | `buff_mg_cooldown` |
| standard | `tech_science_boost` 学術研究推進 | 2 | 100 | agriculture, defense_1 | `buff_science_gen` |
| standard | `tech_automation` ドローン推進器 | 3 | 250 | irrigation | `buff_drone_speed` |
| standard | `tech_chemistry` 農薬・焼夷研究 | 3 | 240 | science_boost, defense_1 | `buff_trap_damage`, `buff_trap_duration` |
| standard | `tech_heavy_artillery` 重火器工学 | 3 | 300 | ballistics | `buff_missile_damage`, `buff_missile_range` |
| standard | `tech_geo_engineering` 環境改変 | 4 | 450 | automation, heavy_artillery | `buff_slow_tile`, `buff_farm_hp` |
| standard | `tech_integrated_defense` 統合防衛指揮 | 4 | 520 | chemistry, geo_engineering | `buff_trap_damage`, `buff_mg_damage`, `buff_farm_hp` |
| expedition | `dig_start` 発掘許可 | 1 | 40 | なし | `buff_money_instant` |
| expedition | `dig_science` 地質調査 | 1 | 70 | なし | `buff_science_gen` |
| expedition | `dig_speed` 掘削機構 | 2 | 130 | dig_start | `buff_drone_speed` |
| expedition | `dig_luck` 幸運の地脈 | 2 | 180 | dig_start, dig_science | `buff_crop_sell` |
| expedition | `dig_yield` 資源精製 | 3 | 280 | dig_speed, dig_luck | `buff_money_instant` |
| expedition | `dig_master` 深層発掘 | 4 | 420 | dig_yield | `buff_science_gen`, `buff_money_instant` |
| expedition | `dig_network` 地層ネットワーク | 4 | 460 | dig_master, dig_science | `buff_trap_duration`, `buff_science_gen` |

標準、探検・発掘、大量出現・総力戦、ドローン強化、タイル罠強化の各ビルドは、上記の基礎研究に続くTier 5〜10の研究を持つ。Tier 5以降は専用の固有バフを使い、研究コストは原則としてTierとともに増加する。Tier 10は複数の高出力バフを同時に付与する最終研究とする。各ビルドの詳細な名称・依存関係・バフIDは`param.js`のテックツリー定義を正とする。

CPレギュ2（`coin_pusher_2`）には、標準開拓ツリー、探検・発掘ツリーに加えて、ドローン購入費を下げるドローン強化系（第3ツリー）と、タイル罠作成費を下げるタイル罠強化系（第4ツリー）を持つ。タイル罠系の専用バフは `buff_discount_tile_trap` で、タイル型罠の作成価格にのみ適用される。

CPレギュ3（`coin_pusher_3`）はCPレギュ2の難易度・初期資金・Wave設定を複製し、開始時から同じ4本のテックツリーを持つ。

基本バフはマシンガンダメージ、作物売却額、ドローン速度、ミサイル射程、畑HP、マシンガン攻撃速度、ミサイルダメージ、作物成長速度、スロー効果、科学生成、即時資金。Tier 5以降の専用バフも同じ効果タイプへ集約し、効果は同じタイプを合算して、通常は `1+合計値` として適用する。研究専用バフはWave報酬の候補には含めない。

基本バフの初期値は次のとおり。

| ID | 効果タイプ | 効果量 | レアリティ |
|---|---|---:|---:|
| `buff_mg_damage` | `mg_damage` | 0.20 | R1 |
| `buff_crop_sell` | `crop_sell` | 0.25 | R1 |
| `buff_drone_speed` | `drone_speed` | 0.30 | R1 |
| `buff_missile_range` | `missile_range` | 0.20 | R2 |
| `buff_farm_hp` | `farm_hp` | 0.50 | R2 |
| `buff_mg_cooldown` | `mg_cooldown` | 0.15 | R2 |
| `buff_missile_damage` | `missile_damage` | 0.25 | R2 |
| `buff_crop_speed` | `crop_speed` | 0.20 | R1 |
| `buff_slow_tile` | `slow_tile` | 0.15 | R3 |
| `buff_science_gen` | `science_gen` | 0.50 | R2 |
| `buff_money_instant` | `instant_money` | 300 | R1 |

追加で89個の拡張バフ定義が生成される。割引、Wave発動、Wave終了、HP、リロール、持ち越し等の定義とイベント連携が含まれるが、全ての定義タイプが独立したゲーム効果として実装されているわけではない。実際にランタイムへ接続されているのは、通常建物の建築割引、ドローン購入割引、アップグレード割引、発動時回復、Wave中の発動回数・一時ステータス、少数建築時の追加取得、攻撃命中時の発動カウントである。なお、取得時最大HP増加用のイベント処理は定義されているが、通常のWave報酬選択処理からは呼び出されないため、Wave報酬として取得しても最大HPは変化しない。

バフの基本参照は `buff(type) = 所持中バフのeffectAmount合計`、`mult(type)=1+buff(type)`。マシンガン攻撃間隔だけは `baseCooldown × (1-buff('mg_cooldown'))`、スロータイル上の敵速度は `slowTileEffect / mult('slow_tile')` を使用する。効果量・レアリティは設定画面から変更できる。

拡張バフ配列の追加フィールドは `[id, name, effectType, effectAmount, description, rarity, metadata]`。定義生成時点では、持ち越し、リロール、選択肢置換、敵関連ステータスなども作られるが、対応する消費処理・表示処理がないタイプは取得しても実際のゲーム状態を変更しない。内部のプレイヤーHP/最大HPランタイムも存在するが、畑HPとは別で、通常の勝敗条件には接続されていない。

## 9. 難易度とレギュレーション

難易度は1〜5。各レギュレーションは名称、各難易度の初期資金、Wave別敵種類・出現数、レギュレーション固有テックツリーを持つ。`CONFIG.maxWave`は共通設定である。

- 初期資金はリセット時だけ再計算。
- 建築、ドローン、建物強化、畑強化の価格は表示と購入の双方で共通の固定値を使用する。
- ヘッダーのゲームレベル変更UIでレベルを変更すると、確認表示なしで即座にゲームをリスタートする。リスタート時に選択したレベルを適用し、敵、建物、資金などのゲーム状態を初期化する。
- レギュレーションまたは難易度の変更時はゲームをリセットし、テックツリー選択状態を初期化する。編集中の設定データは保持する。

初期状態のゲームレベルは1である。`savedConfig`が空の場合、レベル1〜5は初期資金501、Wave 1〜`CONFIG.maxWave`は`peasant: 10`、`adventurer: 5`で初期化される。ゲームレベル変更UIおよび設定画面の「現在の難易度」は `currentDifficultyLevel`、実際の進行中ゲームに適用済みの値はリセット時にコピーされた `activeDifficultyLevel` で管理する。ゲームレベル変更時は即時リセットされるため、変更後は選択レベルが進行中ゲームへ適用される。

数値は入力箇所ごとに非負整数または非負数として正規化される。難易度レベルは1〜5へ限定される。同タイプのドローンを`n`台所有している場合の購入価格は`floor(costDrone × 1.5^n)`である。

## 10. 設定編集と出力

パラメータ画面では、数値の直接編集やスライダー編集によりゲーム中の設定を変更できる。敵の追加・削除、能力設定、Wave出現数、罠の追加・編集・削除、テックツリーと研究の編集、バフ効果量・レアリティ、発掘設定、Wave報酬設定を提供する。

`exportConfig()` は次の項目を含む `let savedConfig = ...;` を生成し、クリップボードへコピーする。

共通設定（`CONFIG`、敵・罠・バフ・ドローン・Waveバフ・スプライト）と、`REGULATIONS`、`activeRegulationId`、`currentDifficultyLevel`を出力する。`trap.js`はレギュレーションデータを保持しない。

ページ読み込み時に `param.js` の `savedConfig` が空でなければ、`applySavedConfig()` が設定を読み込む。エクスポート結果を利用する場合は、生成された `let savedConfig = ...;` で `param.js` の内容を置き換えてから再読み込みする。ブラウザのストレージやサーバーへの自動保存は行わない。

## 11. メインループ

ゲーム画面の「進行速度」セレクターで、1x、2x、4x、8x、16xをゲーム中に変更できる。初期値は1xである。`requestAnimationFrame` で約15ms以上経過したとき、選択した倍率の回数だけ畑、建物、ドローン、敵、弾、エフェクト、罠の更新処理を連続して実行し、その後にCanvasを1回描画する。したがって、2x/4x/8x/16xでは移動、攻撃、作物成長、Wave出現タイマーなどのゲーム進行がそれぞれの倍率で速くなる。表示上のテストフレーム番号は描画更新回数を表し、倍率分は増加しない。無効な値は1xに正規化する。

速度変更は現在のゲーム状態をリセットせず、次の更新から反映する。リセット・再開始後も選択中の速度は維持される。速度セレクターは通常画面とテスト用DOMの双方で同じ更新APIを使用する。

## 12. 応用パラメータ統合テスト（test.jsの仕様）

`test.html`から`test.js`を読み込んで実行する、現行リポジトリのテスト仕様である。実行方法、ログ形式、シナリオ件数の基準は`TEST.md`を正とする。

### 12.1 目的と対象範囲

`test.js` はゲームの勝敗、基本操作、Canvas描画、通常の建築操作を検証するテストではない。設定可能な応用パラメータが、実際のゲームランタイム上で意図した対象・値・時間・範囲に反映されることを確認する統合テストである。

テストは次の順序で段階的に実装・実行する。

1. 敵
2. 罠（罠範囲、罠効果、罠範囲と罠効果の組み合わせ）
3. バフ

各段階でテストケースを1つずつ追加し、既存ケースが再現可能な状態で成功することを確認してから次のケースへ進む。全段階を一度に実装する必要はない。

### 12.2 共通テスト環境

全テストケースは、実際のゲームコードを読み込んだブラウザ実行環境で実行する。`test.js` はゲーム本体とは別のテスト用スクリプトとして読み込み、テスト用の設定変更・状態初期化・更新フレーム制御・観測・ログ出力を担当する。

共通の初期条件は次のとおりとする。

| 項目 | 固定値・ルール |
|---|---|
| マップ | `straight`（中央直線） |
| スポーン地点 | `[7,0]` |
| 基地 | `[7,29]` |
| 難易度 | レベル1 |
| Wave | Wave 1相当 |
| `blockedTileSpawnRate` | `0` |
| 敵出現数 | 検証対象を原則1体。複数対象の範囲テストだけ必要数を配置 |
| 通常攻撃 | マシンガン・ミサイルを設置しない |
| ドローン | 検証に不要な場合は停止または無効化 |
| バフ抽選 | バフテスト以外は無効化。バフテストでは対象を直接指定 |
| 時間 | 実時間ではなく、テスト側が指定フレーム数だけ更新 |

ゲーム状態、乱数状態、テスト用設定はケース開始時に初期化し、ケース終了時に元へ戻す。前のケースで生成された敵、罠、バフ、弾、エフェクト、タイマー、Wave状態を次のケースへ持ち越してはならない。

### 12.3 シード付き乱数

テスト中に使用される乱数は、シードから再現可能な疑似乱数へ置き換える。対象は少なくともランダムblockedマス、敵出現順、バフ抽選、発掘ドローンの結果とする。

次を必須要件とする。

- 各テストケースはシード値を持つ。
- 同一シード、同一設定、同一更新フレーム数では同一結果になる。
- シード値をログに出力する。
- テスト終了時に乱数実装を復元する。
- 効果の単体検証では、不要な乱数要因を設定で無効化する。

シード乱数を導入しても、効果の単体検証をランダム結果に依存させてはならない。特に罠テストでは、blockedマス、敵出現数、バフ、ドローンを固定または無効化する。

### 12.4 テストケースの構造

テストケースは、少なくとも次の情報を持つ。

```text
scenarioId
phase
seed
mapId
enemyIds
trapIds
buffIds
setup
simulationFrames
assertions
```

`scenarioId` は全ログで一意とする。現行`test.js`の`phase`は、`enemy`、`enemy_ability`、`enemy_progression`、`map`、`path`、`wave`、`trap_range`、`trap_effect`、`trap_combination`、`trap_boundary`、`trap_validation`、`buff`、`buff_definition`、`buff_runtime`、`runtime`を使用する。

テストケースは、次のライフサイクルで実行する。

1. シードとテスト用設定を適用する。
2. `straight` マップを初期化する。
3. 必要な敵、罠、バフを配置または生成する。
4. 初期状態を観測して記録する。
5. 指定されたフレーム数だけゲーム更新を実行する。
6. 期待値と実測値を比較する。
7. ケース終了ログを出力する。
8. ゲーム状態、設定、乱数を復元する。

### 12.5 敵テスト

敵テストでは、`ENEMY_DEFINITIONS` に定義された各敵がゲームランタイムへ正しく反映されることを確認する。対象は初期定義の全敵とする。

各敵について次を確認する。

- `enemyId` が一致する。
- `[7,0]` のスポーン地点に生成される。
- HP、最大HP、速度、攻撃力、装甲率、罠耐性が定義値どおりである。難易度倍率は使用しない。
- 定義された能力IDが付与される。
- スポーン地点から基地方向へ移動できる。
- 畑への攻撃が定義された攻撃力で処理される。
- 範囲能力を持つ敵は、設定された射程・間隔・効果量で能力を発動する。

敵能力は通常ステータス検証と分離する。Priest/Wizardの回復、Drummer/Wizardの速度・攻撃バフ、Shieldmasterの正面軽減を個別のシナリオとして扱う。現行コードにFlying Machineの敵定義や罠対象外処理は存在しないため、テスト対象に含めない。

敵テストでは、罠、通常タレット、プレイヤーバフ、ドローンの影響を無効化する。

### 12.6 罠範囲テスト

罠範囲テストでは、効果の数値ではなく、対象選択だけを検証する。罠効果は固定の検出用効果を使用し、範囲内・範囲外・境界上の敵を配置する。

罠タイプごとの期待値は次のとおりとする。

| タイプ | 期待する対象選択 |
|---|---|
| `line` | 罠セル中心から設置方向へ伸びる線分上の敵 |
| `tile` | 罠と同じグリッドマスにいる敵 |
| `area` | 指定半径以内にいる全敵 |

距離の境界上は、現行仕様の距離判定に合わせて対象に含める。範囲外の敵には効果を付与してはならない。敵の罠対象外特性は現行コードには存在しない。

### 12.7 罠効果テスト

罠効果テストでは、敵を確実に罠の対象位置へ配置し、効果量・効果時間・更新処理を検証する。対象効果は `slow`、`poison`、`burn`、`armor_down`、`stun`、`knockback`、`pull` とする。

各効果について次を検証する。

- 適用直後の状態が設定値どおりである。
- 持続時間が設定値どおりである。
- 罠耐性がある敵には効果量と持続時間が割合軽減される。
- `slow` は速度低下率として適用され、`amount` が大きいほど敵の速度が低下する。
- `stun` は敵の通常更新を停止する。
- `poison` と `burn` は指定間隔ごとにダメージを与える。
- `armor_down` は被ダメージ計算に反映される。
- `knockback` と `pull` は設定方向へ即時移動する。
- 同じ効果の再適用は既存効果を置換する。
- 効果終了後に状態が解除される。

### 12.8 罠範囲・効果の組み合わせテスト

実際の罠定義を使い、範囲判定と効果処理が同時に正しく動作することを確認する。

最低限、次を検証する。

- `line` は下方向の線上にいる敵だけに効果を付与する。
- `tile` は同一マスにいる間だけ効果を継続する。
- `area` は半径内の全敵へ効果を付与する。
- 現行コードに定義されている全敵について、範囲内の対象には効果を付与する。
- 毒・火傷の継続ダメージが対象敵だけに発生する。
- クールダウン中に範囲内へ入った敵へ再適用しない。

### 12.9 バフテスト

バフテストでは、バフを「定義として存在すること」と「ゲームランタイムへ接続され実際に状態を変更すること」に分けて扱う。現行リポジトリの `test.js` には、基本バフの定義適用と実効値のシナリオが実装されている。ただし、拡張バフ89個すべての実効効果を網羅するテストではなく、定義適用を確認するシナリオと、実効値を確認する基本バフのシナリオが中心である。

実装済みランタイム効果について、バフ未所持時と所持時を比較し、効果量が設定値どおりになることを確認する。対象には、攻撃力、攻撃間隔、ミサイル射程、畑HP、作物売却額、ドローン速度、作物成長速度、スロー効果、科学生成、即時資金、各種割引、Wave中の効果、最大HP変化を含む。

バフごとに次を記録する。

- バフIDと定義値。
- 適用前の基準値。
- 適用後の実測値。
- 期待される差分または倍率。
- Wave限定効果の適用期間。
- 複数バフの合算・倍率処理。
- バフ取得後およびWave終了後の状態。

定義は存在するがランタイムに接続されていないバフは、実装済み効果の成功テストに混ぜない。「定義あり」「ランタイム接続あり」「効果確認済み」を別々の結果としてログに出力する。

### 12.10 フレーム制御と観測

テストは実時間の待機ではなく、ゲームの更新処理を指定回数呼び出して進行させる。各ケースは、効果付与直後、効果発動時、効果終了直前、効果終了後など、検証に必要なフレームで状態を観測する。

観測対象には、少なくとも次を含める。

- 敵の座標、HP、最大HP、速度、攻撃力、装甲、状態異常一覧。
- 罠の座標、タイプ、クールダウン、定義のコピー。
- バフの所持状態と関連するランタイム値。
- Wave状態、生成状態、現在フレーム。

通常タレット、敵同士の能力、ドローンなど、対象ケースに不要な更新処理は無効化する。ただし、その無効化はログに記録する。

### 12.11 ログ仕様

ログは修正用プロンプトへ直接利用できるJSON Lines形式で出力する。1行を1イベントとし、成功イベントは簡潔に、失敗イベントは修正に必要な状態を詳細に出力する。

各ログには次の共通フィールドを含める。

```text
timestamp
phase
scenarioId
seed
mapId
frame
event
status
expected
actual
```

対象があるイベントでは、`enemyId`、`trapId`、`buffId`、座標、距離を追加する。失敗時には、関連する設定値、罠のクールダウン、敵の状態異常、HP、速度、期待値との差分を追加する。

仕様上のイベント名は次のとおり。ただし、現行`test.js`が実際に出力するイベントは`test_start`、`scenario_start`、`spawned`、`assertion_passed`、`assertion_failed`、`scenario_end`、`test_end`であり、`effect_applied`、`effect_tick`、`effect_expired`、`buff_applied`は現行ログには出力されない。

- `test_start`
- `scenario_start`
- `spawned`
- `effect_applied`
- `effect_tick`
- `effect_expired`
- `buff_applied`
- `assertion_failed`
- `scenario_end`
- `test_end`

通常の`runScenario`ログには`mapId: "straight"`を付ける。決定性・スケジューラ確認では`reset({mapId: "twin_s"})`を使うため、そのシナリオの終了ログは実際の`mapId`（`twin_s`）を付ける。固定値として全ログを`straight`にする仕様ではなく、実行対象マップを記録する。

### 12.12 成功条件と失敗条件

テスト全体は、実行対象の全シナリオのアサーションが成功した場合に成功とする。1つでも期待値と実測値が異なる場合は失敗とし、プロセス終了時の結果にも反映する。

成功条件は「敵・罠・バフの定義が存在すること」ではなく、対象シナリオの実行結果が期待値と一致することである。未実装またはランタイム未接続のバフは、別の未接続結果として明示する。

### 12.13 test.js実装上の制約

`test.js` はテスト対象のゲームロジックを複製して判定してはならない。期待値はテストケースの設定値と `SPEC.md` のルールから作り、ゲーム側の実測状態と比較する。

テスト専用の補助処理を追加する場合も、ゲーム本体の通常動作を変更してはならない。ゲーム状態へアクセスするためのテストフックが必要な場合は、グローバル変数の直接書き換えを増やすのではなく、テスト用の明示的な観測・更新インターフェースとして定義する。

## 13. NEW_SPEC統合結果と実装照合（2026-09-07）

`NEW_SPEC.md`の目的、設定形式、難易度・Wave、レギュレーション切り替え、起動時検証、スプライト、テスト、完了条件を本書へ統合した。現行実装との照合結果は次のとおりである。

### 13.1 実装済み

- `REGULATIONS`で複数レギュレーションを保持し、各レギュレーションに難易度1〜5、初期資金、Wave別敵ID・出現数、テックツリーを持つ。
- Wave開始時は現在のレギュレーション・難易度のWave表を直接展開し、難易度・Waveによる敵HP、速度、攻撃力、出現数の倍率を適用しない。
- 建築、ドローン購入、建物強化、畑強化、罠の価格に難易度倍率を適用しない。
- レギュレーション・難易度の切り替え時にゲームをリセットし、編集データをメモリ上に保持する。リセット時はテックツリーの選択・研究状態を初期化する。
- 全レギュレーション・全難易度について、Wave範囲、未知の敵ID、出現数、Wave合計、初期資金、テックツリーの有無を`validateRegulations()`で検証する。
- `SPRITE_CONFIG`でURI、1インデックスの切り出し開始位置（`sx` / `sy`）、切り出しサイズ、描画サイズ、基準点、フレーム、フレーム間隔、向き反転を保持し、画像読み込み失敗時は対象ごとの図形描画へフォールバックする。`sx: 1, sy: 1`が画像左上のピクセルを示し、横方向の次フレームは` sx + sw `から切り出す。罠定義は任意の` sprite `オブジェクトで共通の`trap`設定を上書きでき、追加罠も個別のスプライトシートを使用できる。
- `test.html`から`test.js`を実行でき、テスト用の初期化・乱数・フレーム更新・観測APIを備える。詳細な基準は`TEST.md`を正とする。

### 13.2 現行実装に残る差異

1. `MAP_DEFINITIONS`と`activeMapId`は`script.js`内の固定データであり、`exportConfig()`の`common`には含まれない。マップ定義の編集・`param.js`への保存・再読み込み復元は未実装で、マップ選択変更とゲームリセットのみ実装されている。
2. `SPRITE_CONFIG`には`excavation`が1項目だけ存在し、NEW_SPECが想定する発掘前・発掘後の別定義はない。発掘前後のマスも専用スプライトでは描画しない。
3. `fallback`文字列を汎用的に解釈する処理はなく、`drawSprite()`失敗時の描画方法は呼び出し側ごとに決まっている。`floor`など、一部の対象は画像失敗時に専用図形を描画しない。
4. 起動時バリデーションは主にレギュレーションとWave表が対象であり、`CONFIG`、マップ、敵能力、共通スプライト、罠定義の全フィールドを網羅検証しない。罠定義は主に設置・保存時に`validateTrapDefinition()`で検証する。

### 13.3 NEW_SPECで定義された実装計画と完了状況

| 項目 | 状況 |
|---|---|
| 設定データ層と`param.js`形式 | レギュレーション、共通設定、後方互換読み込み、出力は実装済み。ただしマップ定義は出力対象外 |
| 起動時バリデーション | レギュレーション・Wave表は実装済み。全共通設定の網羅検証は未実装 |
| 難易度・Wave直接指定 | 実装済み |
| レギュレーション切り替えUIとリセット | 実装済み |
| スプライトロード・描画基盤 | 基盤と一部対象のフォールバックは実装済み。発掘前後と汎用`fallback`解釈は未実装 |
| 地形・建物・罠・敵・ドローンの描画置換 | 一部対象を実装済み。発掘前後の専用描画は未実装 |
| 攻撃・罠エフェクトのイベント描画 | ミサイル爆風、罠効果エフェクト等を実装済み |
| 新仕様のテスト・既存テスト・仕様書更新 | `test.js`、`TEST.md`、本書を実装・更新済み |

上記の未実装項目はNEW_SPECの完了条件を満たさないため、現時点では「NEW_SPECと実装に差異あり」と判定する。本書を統合後の正本とし、`NEW_SPEC.md`は不要な重複仕様書として削除する。

## 14. 現行コードと仕様書の追加照合（2026-09-09）

`script.js`、`trap.js`、`param.js`、`test.js`、`test.html`を再確認した結果、以下を現行実装の正確な状態として追記する。

### 14.1 テスト実装との不一致

- `test.js`はループでシナリオを動的生成する。敵14件、罠・敵能力・Wave・経路・バフ定義適用100件などに横長レイアウト検証を加えた実行予定数は184件で、`expectedScenarioCount`の184と一致する。ソース中の`scenarioId`文字列リテラル数だけを数えてはならない。
- 前回記載していた「実際のシナリオ数24」は、動的生成分を含めていない誤記であるため訂正する。実行結果を確認する場合は、ログの`scenario_end`件数を採用する。

### 14.2 テスト用APIの現行一覧

`window.__TEST_API__`は、設定取得・乱数・速度・初期化・フレーム更新・敵・罠・Wave・建物・畑・バフ・経路・観測のAPIを公開する。主要APIは次のとおり。

`setSeed`、`setSpeed`、`getSpeed`、`reset`、`step`、`getState`、`spawnEnemy`、`placeTrap`、`placeTrapDefinition`、`tryPlaceTrapDefinition`、`setWaveActive`、`setDifficultyConfig`、`setWaveConfig`、`setMaxWave`、`blockCell`、`findPath`、`tryStartWave`、`startWave`、`applyBuff`、`placeBuilding`、`placeFarm`、`removeAllFarms`、`setEnemyHp`、`setEnemyDamage`、`damageEnemy`、`getBuffMetrics`、`getDefinitions`。

通常の`index.html`では`GAME_TEST_MODE`が有効にならないため、`window.__TEST_API__`は公開されない。`test.html`は`__TEST_MODE__`を設定してからゲーム本体と`test.js`を読み込む。

### 14.3 現行実装で仕様化されているが、テストで未網羅の領域

- テックツリーの選択、研究前提、研究解除、研究エディタの編集。
- レギュレーションの追加・名称変更・削除、レベル切り替え、設定編集内容の保持。
- `exportConfig()`のクリップボード出力、`applySavedConfig()`による再読み込み。
- 発掘ドローンの待機、発掘間隔、成功率、資金・科学・バフの抽選。
- Wave報酬候補のレアリティ重み、取得数、`instant_money`、Waveイベント連携。
- 全26項目の`SPRITE_CONFIG`、画像ロード失敗時の図形フォールバック、アニメーションフレーム。
- 全9マップの形状・障害物・複数スポーンと、ランダムblockedマスの経路維持条件。

これらはコード上の機能として存在するが、現行`test.js`の184シナリオは主に敵・罠・Wave・基本バフ・横長レイアウトの統合確認であり、設定編集UI、発掘ドローンの確率分岐、スプライト描画、拡張バフの個別実効効果までは完全には網羅していない。したがって「仕様に記載済み」と「自動テストで網羅済み」は別の状態として扱う。

### 14.4 仕様と実装の既知の差異

- `difficultyMultiplier()`は常に1を返すため、難易度別の敵HP・速度・攻撃力補正はない。一方、全Waveに共通して`CONFIG.waveDensityMultiplier`（初期値1.25）が敵数へ掛かる。難易度倍率用の編集UIの一部コードは残っているが、現行のレギュレーションデータと敵能力には適用されない。
- `startWave`はファイル前半の空定義を後半で実装に置き換える構成であり、最終的にはマップのスポーン地点をラウンドロビンで使い、`CONFIG.waveSpawnInterval`（初期値180ms）間隔で生成する。
- `applySavedConfig()`の保存対象に`MAP_DEFINITIONS`は含まれない。マップ定義は`script.js`固定で、保存・編集対象は共通設定、レギュレーション、敵、罠、バフ、発掘、Waveバフ、スプライトである。
- `validateRegulations()`はレギュレーション、難易度、Wave、敵ID、出現数、テックツリーを起動時に検証するが、`CONFIG`全項目、全マップ、全敵能力、全スプライト項目を一括検証するものではない。

### 14.5 テスト仕様に対する実装範囲

- `test.js`は基本バフ11種の定義存在を確認し、実効値は即時資金・畑HP・科学生成・攻撃力・売却額・ドローン速度・ミサイル射程・マシンガン間隔・ミサイル攻撃力・作物成長・スローの10項目を`getBuffMetrics()`で確認する。拡張バフ89種は定義を適用できることまでを確認し、各効果の実効値は検証しない。
- `test.js`には建築費・ドローン購入費・アップグレード費の割引、発動時回復、Wave中一時ステータス、リロール、持ち越し、報酬置換、最大HP増加、発掘ドロップの実効値を確認するシナリオはない。これらは仕様上の機能または未接続定義として記録されているが、自動テスト済みとは扱わない。
- `test.js`は`TEST_RUNTIME.random`をテストページ内で差し替えるが、テスト終了時に元の乱数関数へ明示的に復元する処理はない。通常画面とは別ページで実行するためゲーム本体へ影響しないが、仕様12.3の「復元」要件は未実装である。
- `test.js`のログはアサーション単位の結果を記録するが、罠効果の付与・継続ダメージ・期限切れを専用イベントとして記録しない。詳細なイベントログを必要とする場合は、`test.js`のログ仕様拡張が必要である。
## 15. UI・罠アイコン更新（2026-09-11）

- 画面左上のFarm Defenseロゴ／ブランド表示は削除する。
- 「Civ テックツリー」は「テックツリー」に名称変更する。
- プレイヤーが選択できる建築一覧から壁と旧来のスロータイルを削除する。減速効果を持つ罠「減速タイル」は罠として残す。内部の経路計算で使用する`wall`値は残す。
- マップ変更と進行速度は、ゲームレベル選択と同じヘッダーの操作列に表示する。
- 選択モードはWave開始の隣に大きめのボタンとして表示し、建築一覧には重複表示しない。
- ドローン購入ボタンは建築ボタンと同じグリッドレイアウトで表示する。
- 罠定義に`icon`（ロゴ絵文字）を持たせ、パラメータ設定の罠編集画面から変更・保存できる。ゲーム画面の罠ボタンと罠エディタの見出しは設定された絵文字を表示する。
- コインプッシャーレギュレーションは、減速、毒、焼夷、装甲破壊、吸引、撃退の各罠に専用のロゴ絵文字を割り当てる。レギュレーション切り替え時に割り当てを反映する。

## 16. 実装照合・仕様修正（2026-09-16）

`index.html`、`script.js`、`trap.js`、`param.js`、`test.js`、`TEST.md`を再照合した。

- 実行時グリッド、Wave出現数倍率、出現間隔、マップ座標の回転、進行速度のリセット後の扱い、テックツリーの追加ノード、スプライト設定数、テスト件数を現行コードに合わせて訂正した。
- 罠アイコンの編集欄が仕様に対して不足していたため、`renderTrapEditor()`の後段で各罠へアイコン入力欄を追加した。変更値は罠定義へ保持され、ゲーム画面の罠ボタンとエディタ再描画に反映される。
- JavaScript 4ファイルの構文検査と差分の空白検査は成功した。ブラウザ統合テストの最新保存済み結果は [TEST.md](TEST.md) 記載の2026-09-11の成功ログであり、今回の監査ではローカル `file:` URLへの自動遷移が実行環境のポリシーで拒否されたため、再実行結果としては扱わない。
