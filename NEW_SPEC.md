# 今回の仕様変更・実装計画

## 1. 目的

複数のレギュレーションを`param.js`一ファイルで保持し、パラメータ設定画面のタブで切り替えられるようにする。難易度による数値倍率を廃止し、難易度ごとに初期資金とWave別の敵種類・出現数を直接指定する。共通のスプライト設定で地形、ゲームオブジェクト、攻撃・罠エフェクトを描画する。

## 2. 設定の分類

### 共通設定

- `CONFIG`（`maxWave`を含む）
- マップ定義
- 敵定義
- 敵能力設定
- 罠定義
- バフ定義
- 発掘ドローン設定
- Wave報酬設定
- スプライト設定

### レギュレーション固有設定

- レギュレーションID・名称
- 難易度1〜5
- 難易度ごとの初期資金（倍率ではなく実額）
- 難易度ごとのWave別敵出現表
- レギュレーションごとのテックツリー

敵・マップ・罠・バフ・スプライトは全レギュレーション共通とする。

## 3. レギュレーションデータ形式

`param.js`は次の形式を基本とする。

```js
let savedConfig = {
  common: { ... },
  regulations: [
    {
      id: 'standard',
      name: '標準設定',
      difficulties: {
        1: {
          initialMoney: 501,
          waves: {
            1: { peasant: 10, adventurer: 5 }
          }
        }
      },
      techTrees: [ ... ]
    }
  ],
  activeRegulationId: 'standard'
};
```

実行時には後方互換を考慮しつつ、共通設定と選択中レギュレーションの設定を既存ランタイムへ適用する。

## 4. 難易度とWave

- 難易度倍率方式を廃止する。
- 建築、ドローン、建物強化、畑強化の価格は共通`CONFIG`の固定値を使う。
- 敵HP、速度、攻撃力に難易度・Wave倍率を適用しない。
- 敵出現数の倍率を適用しない。
- 難易度ごとのWave表に記載された敵IDと数を直接生成する。
- 難易度は1〜5とする。
- 各レギュレーションの各難易度について、Wave 1〜`CONFIG.maxWave`を必須とする。
- 各Waveは合計1体以上の敵を必須とする。
- 空オブジェクト、未記載Wave、存在しない敵ID、不正な出現数、`maxWave`超過は起動エラーとする。
- 出現数は0以上の整数とし、0体の敵を表として記載することは許可する。ただしWave合計は1以上とする。

## 5. レギュレーション切り替え

- パラメータ画面上部に共通設定とレギュレーションタブを表示する。
- タブ切り替え時、現在の編集内容はメモリ上で保持する。
- レギュレーション切り替え時はゲームをリセットする。
- ゲームリセット時、テックツリー選択状態は初期値へ戻す。
- ページ再読み込みで編集内容を保持するには`param.js`へ出力する。

## 6. 起動時バリデーション

`param.js`読み込み後、ゲーム初期化前に全設定を検証する。エラー時はゲームを開始せず、画面に原因を表示する。

画像ファイルの読み込み失敗は起動エラーにせず、設定されたフォールバック表示を使う。

## 7. スプライト

スプライト設定は共通設定とする。対象は以下。

- マップ外、通常床、障害物、スロータイル、スポーン地点、基地
- 発掘前・発掘後
- 畑、建物、罠、敵、ドローン
- ミサイル、爆風、範囲罠、罠効果エフェクト

各定義は必要に応じて以下を持つ。

- `uri`
- 切り出し位置` sx`, `sy`
- 切り出しサイズ` sw`, `sh`
- 描画サイズ` width`, `height`
- 基準点` pivotX`, `pivotY`
- アニメーションフレーム数` frames`
- フレーム間隔` frameDuration`
- 向きによる反転` flipByDirection`
- 読み込み失敗時のフォールバック` fallback`

`frames: 1`でモーションなしとする。オートタイルは使用しない。スプライトは見た目だけを担当し、マップのセル、衝突、経路探索は既存ロジックを使う。

## 8. 実装順

1. 設定データ層と`param.js`形式の再構成
2. 起動時バリデーション
3. 難易度・Wave直接指定への変更
4. レギュレーション切り替えUIとリセット処理
5. スプライトロード・描画基盤
6. 地形、建物、罠、敵、ドローンの描画置き換え
7. 攻撃・罠エフェクトのイベント描画
8. 新仕様のテスト追加・既存テスト更新
9. 実装完了後に`SPEC.md`と`TEST.md`を新仕様へ更新

## 9. 完了条件

- 複数レギュレーションを`param.js`で保持・切り替えできる。
- 切り替え時にゲームが安全にリセットされる。
- 全難易度・全Waveの不正設定を起動時に検出できる。
- 難易度による価格・敵ステータス・出現数の倍率が存在しない。
- 難易度ごとの敵種類・出現数が正しくWaveへ反映される。
- 全対象をスプライトまたは指定フォールバックで描画できる。
- スプライトなし、1フレーム、複数フレーム、画像ロード失敗を扱える。
- テストが成功し、最後に`SPEC.md`と`TEST.md`を更新する。
